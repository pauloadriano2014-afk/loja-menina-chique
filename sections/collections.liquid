{% comment %}
  VERSÃO FINAL BLINDADA:
  - Grid: Mantido (4 colunas PC / Centralizado Mobile)
  - Preço: TRAVADO em Rosa (#FF1493) e Negrito (Removemos o azul de link)
  - Imagens: Ajustadas para não estourar
{% endcomment %}

<div class="page-width collection-container">
  <h1 class="collection-title">{{ collection.title }}</h1>

  <div class="collection-products">
    
    {% for product in collection.products limit: 50 %}
      <div class="collection-product">
        <a href="{{ product.url }}" class="product-link">
          
          <div class="image-wrapper">
            {% if product.featured_image %}
              <img 
                src="{{ product.featured_image | img_url: '500x' }}" 
                alt="{{ product.title | escape }}"
                loading="lazy"
                width="500"
                height="{{ 500 | divided_by: product.featured_image.aspect_ratio | ceil }}"
              >
            {% else %}
              {{ 'product-1' | placeholder_svg_tag: 'placeholder-svg' }}
            {% endif %}
          </div>
           
          <div class="collection-product__content">
            <p class="product-title">{{ product.title | escape }}</p>
            <p class="product-price">{{ product.price | money }}</p>
          </div>
        </a>
      </div>
    {% else %}
      <div style="grid-column: 1 / -1; text-align: center; padding: 40px; border: 1px dashed #ccc;">
        <h3 style="color: #ff007f;">Nenhum produto encontrado.</h3>
        <p>Verifique se as TAGS e o ESTOQUE estão corretos no painel.</p>
      </div>
    {% endfor %}

  </div>
</div>

{% stylesheet %}
  /* Container */
  .collection-container {
    background-color: #FFFFFF;
    padding: 40px 20px;
    max-width: 1200px; 
    margin: 0 auto;
    color: #000000;
  }

  .collection-title {
    text-align: center;
    color: #000000;
    text-transform: uppercase;
    font-size: 2rem;
    margin-bottom: 40px;
    letter-spacing: 2px;
    border-bottom: 2px solid #FF1493;
    display: inline-block;
    padding-bottom: 10px;
    width: 100%;
  }

  /* Grid */
  .collection-products {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr)); 
    gap: 30px;
    width: 100%;
  }

  /* Card */
  .collection-product {
    background-color: #FFFFFF;
    border: 1px solid #F0F0F0;
    border-radius: 8px;
    transition: all 0.3s ease;
    overflow: hidden;
    height: 100%;
  }

  .collection-product:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.05);
    border-color: #FFC0CB;
  }

  /* --- CORREÇÃO DO LINK --- */
  .product-link {
    text-decoration: none !important; /* Remove sublinhado geral */
    color: inherit !important; /* Remove cor azul padrão */
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  /* Imagem */
  .image-wrapper {
    width: 100%;
    aspect-ratio: 3 / 4;
    overflow: hidden;
    background-color: #F9F9F9;
  }

  .image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }

  .collection-product:hover .image-wrapper img {
    transform: scale(1.08);
  }

  /* Texto */
  .collection-product__content {
    padding: 15px;
    text-align: center;
  }

  .product-title {
    font-size: 0.95rem;
    margin: 0 0 8px 0;
    color: #333;
    text-transform: uppercase;
    font-weight: 500;
  }

  /* --- O PREÇO CORRETO --- */
  .product-price {
    font-size: 1.2rem !important; /* Força tamanho */
    font-weight: 700 !important;  /* Força Negrito */
    color: #FF1493 !important;    /* FORÇA ROSA (Mata o azul) */
    text-decoration: none !important; /* Mata o sublinhado */
    margin: 0;
  }
{% endstylesheet %}

{% schema %}
{ "name": "t:general.collection", "settings": [] }
{% endschema %}