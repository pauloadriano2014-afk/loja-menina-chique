{% comment %}
  VERSÃO DEFINITIVA PAULO ADRIANO
  - Design: Recuperado do arquivo original (Rosa/Preto/Branco)
  - Lógica: Simplificada para evitar erro de Paginação
  - Imagens: Tag nativa para evitar erro de snippet
{% endcomment %}

<div class="page-width collection-container">
  
  {% if collection.products_count == 0 %}
    <div style="text-align: center; padding: 40px; background: #fff;">
      <h2 style="color: #ff007f;">Nenhum produto encontrado nesta coleção</h2>
      <p>Verifique se os produtos estão com status ATIVO e com a TAG correta.</p>
    </div>
  {% endif %}

  <h1 class="collection-title">{{ collection.title }}</h1>

  <div class="collection-products">
    
    {% comment %} 
       AQUI ESTÁ A CORREÇÃO: Usamos 'limit: 50' em vez de 'paginate'.
       Isso resolve o erro "Array is not paginateable" instantaneamente.
    {% endcomment %}
    
    {% for product in collection.products limit: 50 %}
      <div class="collection-product">
        <a href="{{ product.url }}" class="product-link">
          
          <div class="image-wrapper">
            {% if product.featured_image %}
              <img 
                src="{{ product.featured_image | img_url: '500x' }}" 
                alt="{{ product.title | escape }}"
                loading="lazy"
                width="500"
                height="{{ 500 | divided_by: product.featured_image.aspect_ratio | ceil }}"
              >
            {% else %}
              {{ 'product-1' | placeholder_svg_tag: 'placeholder-svg' }}
            {% endif %}
          </div>
           
          <div class="collection-product__content">
            <p class="product-title">{{ product.title | escape }}</p>
            <p class="product-price">{{ product.price | money }}</p>
          </div>
        </a>
      </div>
    {% endfor %}

  </div>
</div>

{% stylesheet %}
  /* Container Geral */
  .collection-container {
    background-color: #FFFFFF;
    padding: 40px 20px;
    color: #000000;
  }

  .collection-title {
    text-align: center;
    color: #000000;
    text-transform: uppercase;
    font-size: 2rem;
    margin-bottom: 40px;
    letter-spacing: 2px;
    border-bottom: 2px solid #FF1493; /* Rosa Menina Chique */
    display: inline-block;
    padding-bottom: 10px;
  }

  /* Grid de Produtos */
  .collection-products {
    display: grid;
    /* Grid responsivo: ajusta automaticamente */
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); 
    gap: 20px;
  }

  /* Card do Produto */
  .collection-product {
    background-color: #FFFFFF;
    border: 1px solid #F0F0F0;
    border-radius: 8px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .collection-product:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.05);
    border-color: #FFC0CB; /* Rosa claro no hover */
  }

  .product-link {
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  /* Área da Imagem */
  .image-wrapper {
    width: 100%;
    aspect-ratio: 3 / 4; /* Formato Retrato Padrão */
    overflow: hidden;
    background-color: #F9F9F9;
    position: relative;
  }

  .image-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Garante que a foto preencha tudo sem esticar */
    transition: transform 0.5s ease;
  }
   
  .image-wrapper svg {
    width: 100%;
    height: 100%;
    background-color: #eee;
  }

  .collection-product:hover .image-wrapper img {
    transform: scale(1.08); /* Zoom suave */
  }

  /* Conteúdo (Texto) */
  .collection-product__content {
    padding: 15px;
    text-align: center;
  }

  .product-title {
    font-size: 0.95rem;
    margin: 0 0 8px 0;
    color: #333;
    text-transform: uppercase;
    font-weight: 500;
    line-height: 1.4;
  }

  .product-price {
    font-size: 1.1rem;
    font-weight: 700;
    color: #FF1493; /* PREÇO ROSA */
    margin: 0;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:general.collection",
  "settings": []
}
{% endschema %}