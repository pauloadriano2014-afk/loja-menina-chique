<div class="page-width collection-container">
  
  <h1 class="collection-title">{{ collection.title }}</h1>

  <div class="collection-products-grid">
    
    {% comment %} REGRA: Limite de 50 produtos, sem paginação para não travar {% endcomment %}
    {% for product in collection.products limit: 50 %}
      
      <div class="product-card">
        <a href="{{ product.url }}" class="product-link">
          
          <div class="image-container">
            {% if product.featured_image %}
              <img 
                src="{{ product.featured_image | img_url: '400x500', crop: 'center' }}" 
                alt="{{ product.title | escape }}"
                loading="lazy"
              >
            {% else %}
              {{ 'product-1' | placeholder_svg_tag: 'placeholder-svg' }}
            {% endif %}
          </div>
           
          <div class="product-info">
            <h3 class="product-title">{{ product.title }}</h3>
            <span class="product-price">{{ product.price | money }}</span>
          </div>
        </a>
      </div>

    {% else %}
      <div class="empty-collection-warning">
        <h2>⚠️ Coleção Vazia</h2>
        <p>Nenhum produto encontrado. Verifique se a TAG do produto no painel é igual a regra da coleção.</p>
      </div>
    {% endfor %}

  </div>
</div>

<style>
  /* --- ESTILO FORÇADO (BLINDADO) --- */
  
  .collection-container {
    max-width: 1200px;
    margin: 40px auto;
    padding: 0 20px;
  }

  .collection-title {
    text-align: center;
    font-size: 28px;
    text-transform: uppercase;
    margin-bottom: 40px;
    color: #000;
    border-bottom: 2px solid #ff007f;
    display: inline-block;
    padding-bottom: 5px;
    width: 100%;
  }

  /* O GRID QUE NÃO QUEBRA */
  .collection-products-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr); /* 4 colunas fixas no PC */
    gap: 30px;
    width: 100%;
  }

  /* CARD DO PRODUTO */
  .product-card {
    border: 1px solid #eee;
    border-radius: 8px;
    overflow: hidden;
    background: #fff;
    transition: transform 0.2s;
  }

  .product-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    border-color: #ff007f;
  }

  .product-link {
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  /* IMAGEM CONTROLADA */
  .image-container {
    width: 100%;
    aspect-ratio: 4 / 5; /* Formato retrato padrão */
    overflow: hidden;
    background: #f9f9f9;
  }

  .image-container img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Preenche sem esticar */
    transition: transform 0.3s;
  }
  
  .image-container svg { width: 100%; height: 100%; background: #eee; }

  .product-card:hover img { transform: scale(1.05); }

  /* INFORMAÇÕES */
  .product-info {
    padding: 15px;
    text-align: center;
  }

  .product-title {
    font-size: 14px;
    color: #333;
    margin: 0 0 8px 0;
    text-transform: uppercase;
    font-weight: 600;
    line-height: 1.4;
    min-height: 40px; /* Alinha os preços */
  }

  .product-price {
    font-size: 16px;
    font-weight: bold;
    color: #ff007f;
    display: block;
  }

  .empty-collection-warning {
    grid-column: 1 / -1;
    text-align: center;
    padding: 50px;
    background: #f8f8f8;
    border: 1px dashed #ccc;
  }

  /* RESPONSIVO (CELULAR) */
  @media (max-width: 900px) {
    .collection-products-grid {
      grid-template-columns: repeat(2, 1fr); /* 2 colunas no celular */
      gap: 15px;
    }
    .collection-title { font-size: 22px; }
  }
</style>

{% schema %}
{ "name": "Collection Grid", "settings": [] }
{% endschema %}